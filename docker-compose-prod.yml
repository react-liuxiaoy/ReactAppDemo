version: '3.5'

services:

  sample-app:
    container_name: sample-app-prod
    build:
      context: .
      dockerfile: Dockerfile-prod
    ports:
      - '80:80'
    environment:
      - NODE_ENV=development
